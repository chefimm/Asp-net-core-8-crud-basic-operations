@model IEnumerable<Library.Models.Genre>
@{
    
}
<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" href="/Genre/Add">Daha fazla ekle</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <h3>Türler</h3>
            <table class="table table-stripped table-bordered table-dark">
                <thead>
                    <tr>
                        <th>İsim</th>
                        <th>Eylem</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>
                                <a class="btn btn-success" href="/Genre/Update?id=@item.Id">Düzenle</a>
                                <a class="btn btn-danger" onclick="return window.confirm('Emin misin?')" href="/Genre/Delete?id=@item.Id">Sil</a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>
